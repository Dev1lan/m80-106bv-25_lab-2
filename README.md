# Лабораторная работа №2 - Мини-оболочка с файловыми командами

**Тема:** Разработка мини-оболочки с файловыми командами на Python\
**Вариант:** Easy + Medium (архивы)\
**Студент:** Шамшетов Арыслан Жаксыбаевич\
**Группа:** М8О-106БВ-25\
**Преподаватель:** Самир Ахмед Халид

## Описание проекта

Реализация мини-оболочки (Dev1lan's Shell ) на Python с поддержкой основных файловых операций и работой с архивами. Программа предоставляет интерактивный интерфейс для выполнения файловых команд с логированием всех действий.

## Функциональность

### Обязательная часть (Easy)

- **`ls`** - список файлов и каталогов (поддержка `-l` для подробного вывода)
- **`cd`** - смена рабочей директории (поддержка `..`, `~`)
- **`cat`** - вывод содержимого файла
- **`cp`** - копирование файлов и каталогов (с опцией `-r` для рекурсивного копирования)
- **`mv`** - перемещение и переименование файлов/каталогов
- **`rm`** - удаление файлов и каталогов (с опцией `-r` и подтверждением для директорий)
- **Логирование** - все команды и ошибки записываются в файл `shell.log`

### Дополнительная часть (Medium)

- **`zip`** - создание ZIP архивов
- **`unzip`** - распаковка ZIP архивов
- **`tar`** - создание TAR.GZ архивов
- **`untar`** - распаковка TAR.GZ архивов

## Структура проекта

```
PV_LABA_2/
├── src/
│   ├── main.py                 # Главный файл приложения
│   ├── commands/               # Модули команд
│   │   ├── cat.py              # Команда cat
│   │   ├── cd.py               # Команда cd
│   │   ├── cp.py               # Команда cp
│   │   ├── ls.py               # Команда ls
│   │   ├── mv.py               # Команда mv
│   │   ├── rm.py               # Команда rm
│   │   ├── zip_tar.py          # Команды работы с архивами
│   │   └── __init__.py         # Инициализация пакета команд
│   └── core/                   # Основные модули
│       ├── parser.py           # Парсер команд
│       ├── path_utils.py       # Утилиты для работы с путями
│       ├── logger.py           # Система логирования
│       └── __init__.py         # Инициализация пакета core
├── tests/                      # Юнит-тесты
│ ├── __init__.py               # Инициализация пакета тестов
│ └── test.py                   # Тесты (31 шт)
├── shell.log                   # Файл логов (создается автоматически)
├── pyproject.toml              # Конфигурация проекта
├── uv.lock                     # Файл зависимостей
├── requirements.txt            # Cписок всех необходимых библиотек и их версий
└── README.md                   # Документация
```


## Запуск
```
cd src
python main.py
```

## Использование
```
dev-1-lan:~₽ ls -l
dev-1-lan:~₽ cd ~
dev-1-lan:~₽ cat file.txt
dev-1-lan:~₽ cp -r source_dir destination_dir
dev-1-lan:~₽ zip folder archive.zip
dev-1-lan:~₽ mai
dev-1-lan:~₽ exit
```

## Тестирование

### Запуск тестов
```
python -m pytest tests/ -v
```

### Покрытие кода - 84%
```
pytest tests/test.py --cov=src --cov-report=term-missing
```

## Особенности реализации

- **Модульная архитектура** - каждая команда в отдельном файле
- **Универсальная система путей** - единые функции для разрешения и валидации путей
- **Безопасность** - защита от удаления системных директорий
- **Логирование** - запись всех действий в формате `[1986-04-26 21:23:47] команда`
- **Обработка ошибок** - (имеются) сообщения об ошибках
- **Поддержка специальных символов** - обработка `~`, `.`, `..` в путях

## Технические детали

- Использование `pathlib` для кроссплатформенной работы с путями
- Применение `shutil` для файловых операций
- Модуль `logging` для системы логирования
- Чистая архитектура с разделением ответственности

## Логирование

Все команды пользователя записываются в файл `shell.log` в формате:
```
[1986-04-26 21:23:47] ls -l /home/user
[1986-04-26 21:23:52] cd /nonexistent - ERROR: Directory does not exist
```



```
Made by Dev1lan 🐍
Здесь могла быть ваша реклама
```
Всё изменил, подправил относительно 'issue'. Версия актуальна.
